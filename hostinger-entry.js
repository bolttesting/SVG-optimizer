/**
 * Hostinger / Node.js “Application startup file” entry (project root).
 * Use this path in hPanel if the panel wants a single .js file instead of `npm run start`.
 * Do NOT point the panel at `.next/standalone/server.js` unless you set HOSTNAME=127.0.0.1 in env.
 */
require('./scripts/next-start.cjs')
